# 🚀 Railway 部署完成步骤

## ✅ 本地准备已完成！

我已经为你完成了：
- ✅ Git 仓库初始化
- ✅ 所有文件已提交
- ✅ 代码已适配 Railway

---

## 📋 接下来需要你完成的步骤

### 第一步：在 GitHub 创建仓库

1. **访问：** https://github.com/new

2. **填写信息：**
   - Repository name: `CreditPilot`
   - Description: `信用卡账单管理系统`
   - 选择 Public 或 Private
   - **不要勾选** "Initialize this repository with a README"
   - **不要勾选** "Add .gitignore"
   - **不要勾选** "Choose a license"

3. **点击 "Create repository"**

4. **复制仓库地址**（会显示类似：`https://github.com/你的用户名/CreditPilot.git`）

---

### 第二步：推送代码到 GitHub

**打开终端，执行以下命令：**

```bash
cd /Users/1491-3913zee/Projects/CreditPilot

# 添加远程仓库（替换为你的GitHub用户名和仓库地址）
git remote add origin https://github.com/你的GitHub用户名/CreditPilot.git

# 推送到GitHub
git branch -M main
git push -u origin main
```

**如果遇到认证问题：**
- 使用 GitHub Personal Access Token
- 或者使用 SSH：`git@github.com:你的用户名/CreditPilot.git`

---

### 第三步：在 Railway 部署

1. **访问 Railway**
   - 网址：https://railway.app
   - 点击 "Start a New Project"
   - 用 GitHub 账号登录（推荐）

2. **部署项目**
   - 点击 "Deploy from GitHub repo"
   - 授权 Railway 访问你的 GitHub（如果第一次）
   - 选择你的 `CreditPilot` 仓库
   - Railway 会自动开始部署

3. **等待部署完成**
   - 通常需要 2-5 分钟
   - Railway 会自动生成域名（如：creditpilot.railway.app）

---

### 第四步：配置环境变量和数据库

#### 4.1 添加 PostgreSQL 数据库

1. 在 Railway 项目中，点击 "New"
2. 选择 "Database" → "PostgreSQL"
3. Railway 会自动创建数据库并添加 `DATABASE_URL` 环境变量

#### 4.2 配置环境变量

在 Railway 项目设置 → Variables 中添加以下环境变量：

```
SENDER_EMAIL=business@infinite-gz.com
SENDER_PASSWORD=gracgnrwghbeocox
RECIPIENT_EMAIL=wang041396@gmail.com
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587
UPLOAD_DIR=/app/uploads
REPORTS_DIR=/app/reports
```

**添加方法：**
1. 在 Railway 项目中，点击项目名称
2. 点击 "Variables" 标签
3. 点击 "New Variable"
4. 逐个添加上面的环境变量

#### 4.3 配置文件存储（Volume）

1. 在 Railway 项目中，点击 "New" → "Volume"
2. 创建两个 Volume：
   - **第一个：** Name: `uploads`，Mount Path: `/app/uploads`
   - **第二个：** Name: `reports`，Mount Path: `/app/reports`

#### 4.4 配置定时任务（每晚10点发送邮件）

**重要：** Railway 使用 UTC 时间，马来西亚时间（UTC+8）的晚上10点 = UTC 下午2点

1. 在 Railway 项目中，点击 "New" → "Cron Job"
2. 设置：
   - **Name:** `daily-reminder`
   - **Schedule:** `0 14 * * *`（UTC 下午2点 = 马来西亚晚上10点）
   - **Command:** `cd backend && python3 reminder_system.py`

#### 4.5 初始化数据库

1. 在 Railway 项目中，点击服务名称
2. 点击 "View Logs" 或 "Terminal"
3. 在 Terminal 中执行：
   ```bash
   cd backend && python3 init_db.py
   ```

---

## ✅ 部署完成检查

### 1. 检查服务是否运行

访问你的 Railway 域名：
- Railway 会在项目页面显示域名（如：creditpilot.railway.app）
- 访问：https://你的域名.railway.app
- 应该看到：`{"name":"CreditPilot API","version":"1.0.0","status":"running","docs":"/docs"}`

### 2. 检查 API 文档

访问：https://你的域名.railway.app/docs
- 应该看到 Swagger UI 文档页面

### 3. 测试功能

- **查看账单：** https://你的域名.railway.app/api/statements
- **测试提醒：** https://你的域名.railway.app/api/reminders/test
- **上传单据：** 在 API 文档页面上传测试

---

## 🎯 完成！

部署完成后，你就有一个像样的网址了！

**小助理可以通过网址访问系统，上传单据，每天晚上10点自动收到提醒邮件！**

---

## 🆘 如果遇到问题

1. **查看 Railway 日志**（在项目页面）
2. **检查环境变量**是否都配置了
3. **确认数据库**是否已添加
4. **确认 Volume**是否已创建并挂载

---

**所有代码和配置都已准备好，按照上面的步骤操作即可！** 🚀
