# 🚀 Railway 完整部署指南 - 按步骤操作

## ✅ 第一步：本地准备（已完成！）

我已经为你完成了：
- ✅ Git 仓库初始化
- ✅ 所有文件已提交（53个文件）
- ✅ 代码已适配 Railway

---

## 📋 第二步：在 GitHub 创建仓库

### 步骤：

1. **访问：** https://github.com/new

2. **填写信息：**
   - **Repository name:** `CreditPilot`
   - **Description:** `信用卡账单管理系统 - INFINITE GZ`
   - **选择 Public 或 Private**（建议 Private）
   - ⚠️ **不要勾选** "Initialize this repository with a README"
   - ⚠️ **不要勾选** "Add .gitignore"
   - ⚠️ **不要勾选** "Choose a license"

3. **点击 "Create repository"**

4. **复制仓库地址**
   - 会显示类似：`https://github.com/你的用户名/CreditPilot.git`
   - 或者：`git@github.com:你的用户名/CreditPilot.git`

---

## 📤 第三步：推送代码到 GitHub

**打开终端，执行以下命令：**

```bash
cd /Users/1491-3913zee/Projects/CreditPilot

# 添加远程仓库（替换为你的GitHub用户名）
git remote add origin https://github.com/你的GitHub用户名/CreditPilot.git

# 推送到GitHub
git branch -M main
git push -u origin main
```

**如果遇到认证问题：**

**方法1：使用 Personal Access Token**
1. 访问：https://github.com/settings/tokens
2. 点击 "Generate new token (classic)"
3. 勾选 `repo` 权限
4. 生成 token 并复制
5. 推送时使用 token 作为密码

**方法2：使用 SSH**
```bash
git remote set-url origin git@github.com:你的用户名/CreditPilot.git
git push -u origin main
```

---

## 🚂 第四步：在 Railway 部署

### 4.1 访问 Railway

1. **访问：** https://railway.app
2. **点击 "Start a New Project"**
3. **用 GitHub 账号登录**（推荐，最简单）

### 4.2 部署项目

1. **点击 "Deploy from GitHub repo"**
2. **授权 Railway 访问你的 GitHub**（如果第一次，会弹出授权页面）
3. **选择你的 `CreditPilot` 仓库**
4. **Railway 会自动开始部署**

### 4.3 等待部署完成

- 通常需要 2-5 分钟
- Railway 会自动：
  - 检测到 Python 项目
  - 安装依赖
  - 启动服务
  - 生成域名（如：creditpilot.railway.app）

---

## ⚙️ 第五步：配置环境变量和数据库

### 5.1 添加 PostgreSQL 数据库

1. **在 Railway 项目中，点击 "New"**
2. **选择 "Database" → "PostgreSQL"**
3. **Railway 会自动：**
   - 创建 PostgreSQL 数据库
   - 生成 `DATABASE_URL` 环境变量
   - 你的代码会自动使用这个数据库

### 5.2 配置环境变量

**在 Railway 项目设置 → Variables 中添加：**

1. **点击项目名称**（在 Railway 左侧）
2. **点击 "Variables" 标签**
3. **点击 "New Variable"**
4. **逐个添加以下环境变量：**

```
SENDER_EMAIL=business@infinite-gz.com
SENDER_PASSWORD=gracgnrwghbeocox
RECIPIENT_EMAIL=wang041396@gmail.com
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587
UPLOAD_DIR=/app/uploads
REPORTS_DIR=/app/reports
```

**添加方法：**
- 点击 "New Variable"
- 输入变量名（如：`SENDER_EMAIL`）
- 输入变量值（如：`business@infinite-gz.com`）
- 点击 "Add"
- 重复添加其他变量

### 5.3 配置文件存储（Volume）

1. **在 Railway 项目中，点击 "New" → "Volume"**

2. **创建第一个 Volume：**
   - **Name:** `uploads`
   - **Mount Path:** `/app/uploads`
   - 点击 "Add"

3. **创建第二个 Volume：**
   - **Name:** `reports`
   - **Mount Path:** `/app/reports`
   - 点击 "Add"

### 5.4 配置定时任务（每晚10点发送邮件）

**重要：** Railway 使用 UTC 时间，马来西亚时间（UTC+8）的晚上10点 = UTC 下午2点

1. **在 Railway 项目中，点击 "New" → "Cron Job"**

2. **设置定时任务：**
   - **Name:** `daily-reminder`
   - **Schedule:** `0 14 * * *`（UTC 下午2点 = 马来西亚晚上10点）
   - **Command:** `cd backend && python3 reminder_system.py`
   - 点击 "Add"

### 5.5 初始化数据库

1. **在 Railway 项目中，点击服务名称**（通常是项目名）
2. **点击 "View Logs" 或 "Terminal"**
3. **在 Terminal 中执行：**
   ```bash
   cd backend && python3 init_db.py
   ```
4. **应该看到：** `✓ 数据库表创建成功`

---

## ✅ 第六步：检查部署

### 6.1 检查服务是否运行

1. **在 Railway 项目页面，找到域名**
   - 通常在服务名称下方
   - 类似：`creditpilot.railway.app`

2. **访问域名：**
   - https://你的域名.railway.app
   - 应该看到：`{"name":"CreditPilot API","version":"1.0.0","status":"running","docs":"/docs"}`

### 6.2 检查 API 文档

访问：https://你的域名.railway.app/docs
- 应该看到 Swagger UI 文档页面

### 6.3 测试功能

- **查看账单：** https://你的域名.railway.app/api/statements
- **测试提醒：** https://你的域名.railway.app/api/reminders/test
- **上传单据：** 在 API 文档页面上传测试

---

## 🎯 完成！

部署完成后，你就有一个像样的网址了！

**小助理可以通过网址访问系统，上传单据，每天晚上10点自动收到提醒邮件！**

---

## 📱 小助理如何使用

部署完成后，小助理可以：

1. **访问网址：** https://你的域名.railway.app/docs
2. **上传单据：** 在API文档页面上传
3. **查看账单：** 通过API查看所有账单
4. **接收邮件：** 每天晚上10点自动收到提醒邮件（带Excel附件）

---

## 🆘 如果遇到问题

### 问题1：GitHub推送失败

**解决：**
- 使用 Personal Access Token
- 或使用 SSH 方式

### 问题2：Railway部署失败

**检查：**
- Railway 日志（在项目页面查看）
- 依赖是否正确安装
- 环境变量是否配置

### 问题3：数据库连接失败

**解决：**
- 确认已添加 PostgreSQL 数据库
- 在 Railway Terminal 中运行：`cd backend && python3 init_db.py`

### 问题4：定时任务不工作

**检查：**
- Cron Job 的 Schedule 是否正确（`0 14 * * *`）
- 命令是否正确（`cd backend && python3 reminder_system.py`）
- Railway 日志查看执行情况

---

## 💰 费用

**Railway 免费套餐：**
- $5 免费额度/月
- 通常够用（除非流量很大）
- 超出后按使用量付费（很便宜）

**预计费用：** 免费或 $1-2/月

---

## 🎉 完成！

**所有代码和配置都已准备好，按照上面的步骤操作即可完成部署！**

**需要我帮你检查任何步骤吗？** 🚀
